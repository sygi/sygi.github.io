# Install
sudo apt-get install cabal-install
cabal update
stack/cabal install hakyll
stack ghc site.hs

# Build
ghc site.hs

# Run
./site watch

# Deploy
./site build
git checkout master
cp -a _site/. .
git add <new files>
git commit
git push yet master
