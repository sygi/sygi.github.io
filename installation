# Install
wget -qO- https://get.haskellstack.org/ | sh
stack install hakyll

# Build
stack ghc site.hs

# Run
./site watch

# Deploy
./site build
git checkout master
cp -a _site/. .
git add <new files>
git commit
git push yet master
